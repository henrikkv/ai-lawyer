{
  "name": "mock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:json": "json-server --watch db.json --port 3001",
    "dev": "node ./dist/mock-server.js",
    "build": "tsc --project tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/json-server": "^0.14.7",
    "json-server": "^1.0.0-beta.3"
  }
}
